@page "/StorageForm"
@inject NavigationManager NavManager
<style>
    body{
        background-image:url('./images/background.jpg');
        background-size:cover;
        background-repeat:repeat;
    }
</style>
<div style="display: flex; align-items:center; justify-content:center; margin: auto;">
<MudPaper Class="pa-13 ma-1">
<MudForm @ref="@form">
    <div>
    <MudText Color="Color.Primary" Style="margin-bottom:15px" Typo="Typo.h5">Add Storage record</MudText>
    <MudText></MudText>
    <MudText></MudText>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Serial Number" Variant="Variant.Outlined" Required/>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Model" Variant="Variant.Outlined" Required/>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Bus Type" Variant="Variant.Outlined" Required/>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Media Type" Variant="Variant.Outlined" Required/>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Health Condition" Variant="Variant.Outlined" Required/>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Friendly Name" Variant="Variant.Outlined" Required/>
    <MudTextField Color=Color.Primary T="String" Style="margin-bottom:5px" Label="Size" Variant="Variant.Outlined" Required/>
     <MudRadioGroup T="string" Required="true">
         <MudRadio Option="@("Active")">Active</MudRadio>
         <MudRadio Option="@("InActive")">InActive</MudRadio>
    </MudRadioGroup>
    <MudText></MudText>
    <MudText></MudText>
    <MudText></MudText>
    <MudButton Style="margin:9px" Variant="Variant.Filled" OnClick="@(async () => await Submit())" Class="justify-content-center align-content-center" EndIcon="@Icons.Material.Filled.Send" Color="Color.Primary">Submit</MudButton>
    <MudButton Style="margin:9px" Variant="Variant.Filled" OnClick="Redirect" StartIcon="@Icons.Material.Filled.Delete" Color="Color.Error">Delete</MudButton>
    </div>
</MudForm>
</MudPaper>
</div>
@code {
    void Redirect()
    {
        NavManager.NavigateTo("/Assets");
    }

    [Inject] ISnackbar Snackbar { get; set; }
    MudForm form;
    private async Task Submit()
    {
        await form.Validate();

        if (form.IsValid)
        {
            Snackbar.Add("Submitted!", Severity.Success);
            Redirect();
        }
    }

}